-- strategy_report_last_90s dump for window_id LIKE %26MAR052245%
CREATE TABLE IF NOT EXISTS strategy_report_last_90s (
  order_id TEXT PRIMARY KEY,
  window_id TEXT,
  asset TEXT,
  ts_utc TEXT,
  ticker TEXT,
  side TEXT,
  spot REAL,
  strike REAL,
  resolution_price INTEGER,
  seconds_to_close REAL,
  distance REAL,
  min_distance_threshold REAL,
  bid INTEGER,
  limit_or_market TEXT,
  price_cents INTEGER,
  placed INTEGER,
  filled INTEGER,
  fill_price INTEGER,
  final_outcome TEXT,
  pnl_cents INTEGER,
  is_stop_loss INTEGER,
  stop_loss_sell_price INTEGER,
  skip_reason TEXT,
  skip_details TEXT,
  is_filled INTEGER,
  spot_kraken REAL,
  spot_coinbase REAL,
  distance_kraken REAL,
  distance_coinbase REAL,
  min_dist_after_placement REAL,
  min_bid_after_placement INTEGER,
  post_placement_history TEXT,
  pre_placement_history TEXT
);
