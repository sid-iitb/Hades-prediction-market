# DEPRECATED: Root config.yaml is no longer used.
#
# This file previously had order.hourly.max_cost_cents: 5000 ($50), which caused
# larger-than-intended orders. It has been archived to config/config.yaml.archived.
#
# Use the split config instead (default):
#   python -m bot.main --config config/config.yaml
#
# Do not use: --config config.yaml
_legacy_root_config_deprecated: true
